import Link from "next/link";
import {mockCreator,mockProducts} from "@/lib/mock";
const won=(n:number)=>new Intl.NumberFormat("ko-KR").format(n)+"원";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const c=mockCreator; return <div className="profile"><div className="glass"><div className="avatar">CH</div><div className="name">{c.display_name}</div><div className="muted">{c.bio}</div><p style={{lineHeight:1.7}}>{c.intro}</p><div className="badges"><span className="badge">✓ 본인 인증</span><span className="badge">답변률 {c.answer_rate}%</span><span className="badge">평균 {c.avg_response}</span></div></div><div className="section">질문하기</div>{mockProducts.map(p=><Link className="product" href={`/ask/${slug}?product=${p.id}`} key={p.id}><div className="emoji">{p.emoji}</div><div className="grow"><div className="title">{p.title}</div><div className="small">{p.description}</div></div><div className="price">{won(p.price)}</div></Link>)}</div>}
